import styled from "styled-components";

import { Error as ErrorColor } from "../../../assets/styles/colors";

export const Error = styled.small `
  color: ${ErrorColor};
  font-weight: 600;
  margin-top: 7px;
`